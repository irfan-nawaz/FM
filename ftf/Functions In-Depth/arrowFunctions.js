var nameImprover = (name, adj) => {
return `Col ${name} Mc ${adj} pants`;
};

$('body').hide();

myArr.forEach(val => console.log(val));

$('button').on('click', () => {
console.log('Don\'t press my buttons!');
});


// myArr.forEach(val => {return val + 1} =>  {return 3});
// this passing one function return to another function arguments gets weird with arrow functions